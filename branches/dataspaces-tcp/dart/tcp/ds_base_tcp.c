#include "ds_base_tcp.h"

struct dart_server *ds_alloc(int num_sp, int num_cp, void *dart_ref) { return NULL; }
void ds_free(struct dart_server* ds) {}
int ds_process(struct dart_server* ds) { return 0; }

